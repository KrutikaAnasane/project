#include <stdio.h>
#include "math.h.c"

int main() {
int a, b;
 	double i, y, z;
 	scanf("%d%d", &a, &b);
 	//y = absolute(i);
 	z = power(a, b);
 	//printf("abs value is %lf\n", y);
 	printf(" power of i is %lf\n", z);
}
