#include <stdio.h>
#include "maths.h"

double absolute(double x) {
	if(x > 0) 
		return x;
	else if(x == 0) 
		return 0;
	else 
		return (-x);
}

int main() {
 	double i, y;
 	scanf("%lf", &i);
 	y = absolute(i);
 	printf("abs value is %lf\n", y);
}
