#include <stdio.h>
#include "math.h.c"

int main() {
	long double a;
 	long double z;
 	scanf("%Lf", &a);
 	//y = absolute(i);
 	z = squrtl(a);
 	//printf("abs value is %lf\n", y);
 	printf(" square root is %Lf\n", z);
}
